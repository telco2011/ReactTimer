var React = require('react');

var Timer = React.createClass({
  render: function () {
    return (
      <div>
        <p>Timer.jsx Component!</p>
      </div>
    );
  }
});

module.exports = Timer;